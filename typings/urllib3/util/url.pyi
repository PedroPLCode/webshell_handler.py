"""
This type stub file was generated by pyright.
"""

from collections import namedtuple

url_attrs = ...
NORMALIZABLE_SCHEMES = ...
PERCENT_RE = ...
SCHEME_RE = ...
URI_RE = ...
IPV4_PAT = ...
HEX_PAT = ...
LS32_PAT = ...
_subs = ...
_variations = ...
UNRESERVED_PAT = ...
IPV6_PAT = ...
ZONE_ID_PAT = ...
IPV6_ADDRZ_PAT = ...
REG_NAME_PAT = ...
TARGET_RE = ...
IPV4_RE = ...
IPV6_RE = ...
IPV6_ADDRZ_RE = ...
BRACELESS_IPV6_ADDRZ_RE = ...
ZONE_ID_RE = ...
_HOST_PORT_PAT = ...
_HOST_PORT_RE = ...
UNRESERVED_CHARS = ...
SUB_DELIM_CHARS = ...
USERINFO_CHARS = ...
PATH_CHARS = ...
FRAGMENT_CHARS = ...
class Url(namedtuple("Url", url_attrs)):
    """
    Data structure for representing an HTTP URL. Used as a return value for
    :func:`parse_url`. Both the scheme and host are normalized as they are
    both case-insensitive according to RFC 3986.
    """
    __slots__ = ...
    def __new__(cls, scheme=..., auth=..., host=..., port=..., path=..., query=..., fragment=...): # -> Self:
        ...
    
    @property
    def hostname(self): # -> Any:
        """For backwards-compatibility with urlparse. We're nice like that."""
        ...
    
    @property
    def request_uri(self): # -> Any | Literal['/']:
        """Absolute path including the query string."""
        ...
    
    @property
    def netloc(self): # -> LiteralString | Any:
        """Network location including host and port"""
        ...
    
    @property
    def url(self): # -> Any | str:
        """
        Convert self into a url

        This function should more or less round-trip with :func:`.parse_url`. The
        returned url may not be exactly the same as the url inputted to
        :func:`.parse_url`, but it should be equivalent by the RFC (e.g., urls
        with a blank port will have : removed).

        Example: ::

            >>> U = parse_url('http://google.com/mail/')
            >>> U.url
            'http://google.com/mail/'
            >>> Url('http', 'username:password', 'host.com', 80,
            ... '/path', 'query', 'fragment').url
            'http://username:password@host.com:80/path?query#fragment'
        """
        ...
    
    def __str__(self) -> str:
        ...
    


def split_first(s, delims): # -> tuple[Any, Literal[''], None] | tuple[Any, Any, Any | None]:
    """
    .. deprecated:: 1.25

    Given a string and an iterable of delimiters, split on the first found
    delimiter. Return two split parts and the matched delimiter.

    If not found, then the first part is the full input string.

    Example::

        >>> split_first('foo/bar?baz', '?/=')
        ('foo', 'bar?baz', '/')
        >>> split_first('foo/bar?baz', '123')
        ('foo/bar?baz', '', None)

    Scales linearly with number of delims. Not ideal for large number of delims.
    """
    ...

def parse_url(url): # -> Url:
    """
    Given a url, return a parsed :class:`.Url` namedtuple. Best-effort is
    performed to parse incomplete urls. Fields not provided will be None.
    This parser is RFC 3986 and RFC 6874 compliant.

    The parser logic and helper functions are based heavily on
    work done in the ``rfc3986`` module.

    :param str url: URL to parse into a :class:`.Url` namedtuple.

    Partly backwards-compatible with :mod:`urlparse`.

    Example::

        >>> parse_url('http://google.com/mail/')
        Url(scheme='http', host='google.com', port=None, path='/mail/', ...)
        >>> parse_url('google.com:80')
        Url(scheme=None, host='google.com', port=80, path=None, ...)
        >>> parse_url('/foo?bar')
        Url(scheme=None, host=None, port=None, path='/foo', query='bar', ...)
    """
    ...

def get_host(url): # -> tuple[Any | Literal['http'], Any, Any]:
    """
    Deprecated. Use :func:`parse_url` instead.
    """
    ...

